---
title: Java中的软引用和它的测试
date: 2023-09-07 14:42:46
permalink: 2023/09/07/20230907-Java中的软引用和它的测试/
banner_img: /img/230907-Java中的软引用和它的测试.jpg
tags:
  - java
  - jvm
  - reference
categories: Java
---
<div style="font-size: 0.9em; color: #8a8a8a; line-height: 1.7;">

**创作透明度：** 🖋️ 人工创作（完全作者撰写） | ✨ AI润色（作者撰写，AI优化语言） | 🤖 AI辅助（作者主导，AI提供内容/结构建议） | 🛠️ AI+人工（AI生成初稿，作者修改完善） | 🧠 全AI生成（内容完全由AI生成）

**本文标注： 🖋️ 人工创作**

</div>


### Java中的软引用和它的测试

#### 软引用介绍



### 软引用和它的测试

​	将被回收的困难程度排序，应该是强引用>软引用>弱引用。对比软引用和对引用，软引用的回收时间是不可预测的，它仅在内存紧张的GC进行回收。而弱引用会在下一次GC时被回收。

#### 测试设计

​	为了验证它的回收，我们可以创建一个持有SoftReference的list，不断往里塞大对象，不断触发GC，查看结果。注，内存紧张回收后，该对象被回收，但指向该对象的指针没有被回收。

```java
ArrayList<SoftReference<byte[]>> list = new ArrayList<>();
while(true){
	list.add(new SoftReference<byte[]>(new byte[1024 * 100]));
}
```

```java
System.out.println("first data in list: " + list.get(0).get());
```

测试代码：

```java
    @Test
    public void testSoftReference(){
        ArrayList<SoftReference<byte[]>> list = new ArrayList<>();
        new Thread(() -> {
            while(true){
                list.add(new SoftReference<byte[]>(new byte[1024 * 100]));
                try {
                    Thread.sleep(1000);
                } catch (InterruptedException e) {
                    throw new RuntimeException(e);
                }
            }
        }).start();
        new Thread(
            () ->{
                while(true){
                    System.out.println("soft reference list size: " + list.size());
                    System.out.println("first data in list: " + list.get(0).get());
                    try {
                        Thread.sleep(300L);
                    } catch (InterruptedException e) {
                        throw new RuntimeException(e);
                    }
                }
            }
        ).start();

        try {
            Thread.currentThread().join();
        } catch (InterruptedException e) {
            e.printStackTrace();
            System.exit(1);
        }
    }
```

设置JVM参数如下

```
-ea -verbose:gc -Xms4m -Xmx4m -Xmn2m 
```

#### 分析测试结果

```java
System.out.println("first data in list: " + list.get(0).get());
```

​	上面语句不会报空指针异常，这说明GC回收了软引用对象后，并没有回收软引用变量本身，如果希望软引用变量被回收，最好将它添加到reference queue再手动回收。

打印日志如下

```
[Full GC (Ergonomics)  3039K->2586K(3584K), 0.0083289 secs]
[Full GC (Ergonomics)  3039K->2610K(3584K), 0.0100043 secs]
soft reference list size: 1
first data in list: [B@2781fef4
[Full GC (Ergonomics)  3035K->2732K(3584K), 0.0090069 secs]
[Full GC (Ergonomics)  3039K->2896K(3584K), 0.0137084 secs]
[Full GC (Ergonomics)  3029K->3007K(3584K), 0.0065464 secs]
[Full GC (Ergonomics)  3021K->2986K(3584K), 0.0062080 secs]
[Full GC (Ergonomics)  3029K->3010K(3584K), 0.0056912 secs]
[Full GC (Ergonomics)  3028K->3010K(3584K), 0.0059427 secs]
[Full GC (Allocation Failure)  3010K->2704K(3584K), 0.0124300 secs]
soft reference list size: 1
first data in list: null
```

​	通过这段代码我们可以观察到，在GC的初始几次循环中尽管发生了垃圾回收，但是软引用对象还没有被回收。这是因为JVM认为内存空间还充足,可以继续保留软引用对象。但是随着程序运行，内存空间逐渐紧张，在某一次GC过程中，JVM终于决定回收软引用对象以释放更多内存。此时我们打印软引用所引用的对象,其值为null。这表示软引用对象已经被回收了。

​	但需要注意的是，原本指向这个软引用对象的软引用变量list本身还存在，它仍然保持对这个已经被回收对象的引用，只是这个引用现在无效了，如下图。如果程序不及时清理这种无效引用,可能会渐渐占用较多内存。

![230907-软引用变量回收](/img/230907-软引用变量回收.jpg)

#### 软引用变量的回收

​	为了避免上述情况，我们可以主动回收软引用变量。

##### 放入引用队列进行回收

将软引用变量放入引用队列里，在回收的时候手动释放它。将创建对象并塞入对象的代码改为

```java
 ArrayList<SoftReference<byte[]>> list = new ArrayList<>();
 ReferenceQueue<byte[]> referenceQueue = new ReferenceQueue<>();

  while(true){    
      list.add(new SoftReference<byte[]>(new byte[1024 * 100], referenceQueue));
  }
```

​    使用另一个线程不断扫描referenceQueue，当软引用对象被回收时，软引用变量就会被放入referenceQueue中，我们可以通过手动把它设置为null，或者从list中移除它来回收。

##### WeakHashMap进行回收

​	另外，也可以使用WeakHashMap来储存，储存的代码如下

```java
WeakHashMap<SoftReference<byte[]>, byte[]> map = new WeakHashMap<>();
while(true){
	SoftReference<byte[]> obj = new SoftReference<>(new byte[1024 * 100]);
    map.put(obj, obj.get());
    System.out.println("add data in map.");
}
```

命令行输出

```tex
[Full GC (Ergonomics)  3014K->2907K(3584K), 0.0063295 secs]
soft reference map size: 2
first data in map: java.lang.ref.SoftReference@4c67258e=[B@58a828a3
[Full GC (Ergonomics)  2912K->2807K(3584K), 0.0058464 secs]
add data in map.
[Full GC (Ergonomics)  2913K->2807K(3584K), 0.0080876 secs]
add data in map.
[Full GC (Ergonomics)  2912K->2807K(3584K), 0.0067576 secs]
add data in map.
soft reference map size: 1
first data in map: java.lang.ref.SoftReference@2b0d69c0=[B@1daaadd2
```

​	分析日志，可以看到，最开始map中已经有两个对象，后面也在不断往map中插入数据。但经过几轮回收后，map size变为了1。这说明，JVM中间经过了一次内存紧张的回收，将我们建立的软引用变量回收了，而在下一次回收中，weakHashMap识别到它的value被回收，于是主动删除了这个键值对。
