---
title: 当时序图遇到 Agent：一次彻底的模型失配
date: 2026-04-13 23:09:03
permalink: 2026/04/13/20260413-当时序图遇到Agent一次彻底的模型失配/
banner_img: /img/44d47d503b09e7879b9788cdcc98082.jpg
tags:
  - AI
  - Agent
  - 建模
  - 状态机
categories: 工程实践
---

**创作透明度：** 🖋️ 人工创作（完全作者撰写） | ✨ AI润色（作者撰写，AI优化语言） | 🤖 AI辅助（作者主导，AI提供内容/结构建议） | 🛠️ AI+人工（AI生成初稿，作者修改完善） | 🧠 全AI生成（内容完全由AI生成）

**本文标注： 🤖 AI辅助**

在尝试用传统软件工程工具去描述 Agent（特别是 ReAct 模式）时，我产生过一种很明显的违和感，图是能画出来，但总觉得哪里不对。

一开始以为是表达方式的问题，后来发现不是——**是模型本身不兼容。**

这篇文章就试图把这个“违和感”讲清楚。

## **一、时序图的本质：调用链 + 栈模型**

在传统后端开发中，我们最熟悉的是“调用链”。

比如这样一段代码：

```java
public class CallChainDemo {

    public static void main(String[] args) {
        A();
    }

    static void A() {
        System.out.println("enter A");
        B();
        System.out.println("exit A");
    }

    static void B() {
        System.out.println("enter B");
        C();
        System.out.println("exit B");
    }

    static void C() {
        System.out.println("enter C");
        System.out.println("exit C");
    }
}
```

它的执行结构是

```
A
└── B
    └── C
```

这就是一个标准的 **调用栈（Call Stack）模型**，可以把运行过程理解为一个不断“压栈—出栈”的过程，执行路径天然闭合，一定会return。

## **二、Agent的本质：循环 + 状态机**

当我们把这个模型套到 Agent 上，就开始不对劲了。ReAct中的循环最简单明确，我们以ReAct模式为例。

ReAct 的典型流程是：

- 思考（Think）
- 行动（Act）
- 观察（Observe）
- 再思考（Think）
- 再行动……

结构如下：

```
[Think] → [Act] → [Observe]
   ↑          │
   └──────────┘
```

这和调用栈最大的不同是，Agent 不关心“回到哪里”，只关心“下一步去哪”。更准确的类比是，**Agent = 状态机（State Machine）**。

特点：

- 当前状态：由上下文（memory / messages）决定
- 转移规则：由模型 + 决策函数决定
- 没有“调用栈”，只有“状态演进”

## **三、用 LangGraph 看得更清楚**

来看一个典型的 ReAct Agent 实现（简化版）：

```python
from typing import Annotated
from typing_extensions import TypedDict

from langchain_core.messages import BaseMessage
from langchain_core.tools import tool
from langchain_openai import ChatOpenAI
from langgraph.graph import StateGraph, START, END
from langgraph.graph.message import add_messages
from langgraph.prebuilt import ToolNode

# ===================== Agent的状态机 =====================

# 状态：对话历史（也是“记忆”）
class State(TypedDict):
    messages: Annotated[list[BaseMessage], add_messages]

# ===================== Tool定义 =====================

# 工具（Act）
@tool
def get_weather(city: str) -> str:
    return {
        "beijing": "晴，24℃",
        "shanghai": "多云，26℃",
        "hangzhou": "小雨，22℃",
    }.get(city.lower(), "暂无数据")


# ======================= 图节点（Think / Decide） =======================

tools = [get_weather]

llm = ChatOpenAI(
    model="gpt-4.1-mini",
    temperature=0,
).bind_tools(tools)

def think(state: State):
    msg = llm.invoke(state["messages"])
    return {"messages": [msg]}

def should_act(state: State):
    last = state["messages"][-1]
    return "act" if getattr(last, "tool_calls", None) else END


# ============================= LangGraph构建Agent =============================

# 定义一个带环的执行图：Think → Act → Think → ... 直到结束

builder = StateGraph(State)

builder.add_node("think", think)
builder.add_node("act", ToolNode(tools))

builder.add_edge(START, "think")
# Think → Act / END
builder.add_conditional_edges(
    "think",
    should_act,
    {
        "act": "act",
        END: END,
    },
)
# Act → Think（Observe 后进入下一轮）
builder.add_edge("act", "think")


# ============================= 代码运行 =============================

app = builder.compile()
```

## **四、更深一层：两种“程序”的哲学差异**

如果说传统程序是在解决“如何把一条路径执行完”，那么 Agent 程序解决的，是“在图中下一步该走向哪里”。
**前者是路径控制，后者是状态决策。**
